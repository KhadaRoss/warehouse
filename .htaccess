Options -MultiViews
    RewriteEngine On

    RewriteCond %{REQUEST_URI} ^/warehouse/ajax/
    RewriteRule ^ ajax.php [QSA,L]

    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteRule ^ index.php [QSA,L]
